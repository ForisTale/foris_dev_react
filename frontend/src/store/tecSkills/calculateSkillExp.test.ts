import calculateSkillExp from "./calculateSkillExp";

describe("Calculate exp needed to lvl up skill", () => {
  test("from level 15 to 100 for all skills", ()=>{
    const testValues: (string | number)[][] = [["test"]];

  });
});
